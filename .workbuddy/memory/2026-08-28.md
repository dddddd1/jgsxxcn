# 2026-08-28 工作日志

## 选型机市场价基准（用户内部参考，未上页面）
用户问金刚石选型机价格范围，明确要求"不要放在页面上"。结论（2026-08 联网采集）：
- 小型振筛/分级筛（锆珠/氧化锆）：0.8万–3万（8800 / 25800 / 76000 实测）
- 国产标准选型机/选球机/分选机：5万–15万（金刚石形状分选机 5–10万；陶瓷球分选机普通型 8–15万）
- 高精度/进口分选机：15万–25万+（陶瓷球高精度 25万+；德国 SHAPE SORTER 4.1 进口 5–10万 属实验室级）
- 已剔除混入的矿山选矿设备（磁选机/跳汰机），与兴顺磨料设备非同类。
- 站上 jgsxx.cn 首页、产品页均**不展示价格**（用户偏好：价格面议/留资询价）。

## 全站去价格（代码已提交 d07e137，已 push）
用户发现页面有价格，要求去掉。范围：只去具体金额（A+B），保留 FAQ 问题标题含"价格"二字与"精度/价格/应用场景"对比维度词（C1 维度词未动）。

改动（10 文件，+43/-183）：
- 删结构化价格：首页4个Product `offers`、xxj/xqj 产品页 `offers`、16.html `priceRange`、post/60-65 各 `offers`（均含 `price` 字段，会让百度/Google 富媒体结果直接显价，最该删）。
- 重写正文/FAQ 金额：选型机/分选机/纳米分选机价格区间、调试费(2000/5000)、维修费(800/1500)、维护合同(8000-15000/年)、案例成交价(28000/35000)、选型对比价格表、预算数字(1.5万/5万)。
- 修复 index.html 删 offers 后残留尾逗号，校验 10 文件 JSON-LD 全部合法。
- 未动：旧钣金/新能源老文（post/3-49 等 C2，用户选"不动"）；维修文 post/64 等"报价/获取报价"CTA 保留（非披露金额）。

## 首页图片百度收录修复（代码已提交 5838f7d，已 push）
延续 2026-08-19 的 A+B+C 优化（robots放行logo + image-sitemap.xml + ImageObject JSON-LD + 7张真实产品图）。本轮补两处收录增益：
- 7 个首页卡片 `<img>` 全部补齐 width/height（前2张300，后5张按真实像素 1200/675），避免百度判定尺寸不明。
- logo `<img>` src 改为绝对路径 `https://jgsxx.cn/zb_users/.../logo.png`。
- **收录关键仍需用户手动**：百度站长平台(ziyuan.baidu.com)提交 image-sitemap.xml + 首页URL，否则图片无法被发现收录。
- 备注：百度对 image sitemap 扩展协议支持有限，主要靠页面内 `<img>` + alt；本站首页 7 个 `<img>` 均为静态 HTML（非 JS 渲染）、非懒加载，可被抓取。
