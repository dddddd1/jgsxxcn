# 2026-08-19

## 首页图片百度收录优化（jgsxx.cn）

**任务**：让首页的图片能被百度图片搜索收录

**只读诊断发现**：
- 首页 8 个 img：1 个 logo（路径在 `zb_users/theme/filmlee/style/images/`，被 robots 屏蔽）+ 7 个文章卡片缩略图全部 `src="images/external/placeholder.jpg"`（同一张 290x250 真实产品图）
- robots.txt 的 `Disallow: /zb_users/` 是 logo 被屏蔽的根因
- sitemap.xml 无 image 扩展
- 首页 JSON-LD 无 ImageObject
- placeholder.jpg 实际是兴顺选型机真实产品图（含电话+水印），有收录价值

**已落地改动**：
1. `robots.txt`：删除 `Disallow: /zb_users/`，新增 `Sitemap: https://jgsxx.cn/image-sitemap.xml`。logo 图解阻塞。
2. 新建 `image-sitemap.xml`：首页 URL 下挂 2 个 image:image（logo + placeholder.jpg），含 title/caption。
3. `index.html`：在原 JSON-LD 后插入 ImageObject JSON-LD（logo #logo + 产品图 #hero-product-image），含 width/height/description。

**素材瓶颈（待用户）**：
- 7 张卡片同 URL → 百度图片搜索只聚合为 1 个结果。要让 7 张图被独立收录，必须替换为 7 张不同的真实选型机/分选机/筛分机产品图。
- 用户答"已有，稍后发你"，等素材到位再替换。

**百度站长平台下一步**（人工操作）：
- ziyuan.baidu.com 提交 image-sitemap.xml
- 抓取诊断 → 抓取首页验证 logo.png 可被索引
- 等待 1-2 周后观察百度图片搜索收录

**未做（保留原状）**：
- 首页 7 个 alt 文本保持不变（已含关键词，简短合理）
- og:image / twitter:image 仍指向 logo.png（用户在 D 项答"稍后发图"，等替换 7 张卡片时一并改）

---

## 第二轮：用户发了 16 张车间图，落地 7 张差异化

**用户素材**：16 张剪贴板截图（10 张不同内容 + 6 张重复），全部带产品水印。MD5 去重后实际 11 张独立内容。

**只读摸底**（不重新跑诊断）：
- 图片实际内容（按水印识别）：金刚石选型机/选形机、氧化锆珠选型机/分选机/筛分机、陶瓷珠筛分机/选型机发货、陶瓷球筛选机、0.01-10mm 微珠
- 大多数是车间全景实拍（非产品特写），但 1 张"陶瓷珠选型机发货"是卡车装货，差异化强
- 没有现成的"维护保养"或"应用领域 6 大行业"等场景图，仍需凑

**7 张选图**（按 alt 主题匹配度优先）：
| 卡片 | alt | 选图 | 物理尺寸 |
|---|---|---|---|
| 1 | 金刚石选型机工作原理 | jingangshi-xuanxingji-1.jpg | 1200×1200 |
| 2 | 金刚石选型机 | jingangshi-xuanxingji-2.jpg | 1200×1200 |
| 3 | 选型机应用领域 6 大行业案例 | yanghuagao-zhu-xuanxingji.jpg | 1200×1200 |
| 4 | 选型机 vs 选球机 5 大核心差异 | weizhu-001-10mm.jpg | 1200×1200 |
| 5 | 氧化锆珠分选机完全指南 | yanghuagao-zhu-fenxuanji.jpg | 1200×675 |
| 6 | 选型机日常维护保养手册 | taoci-zhu-fahuo.jpg（发货场景）| 1200×1200 |
| 7 | 纳米级分选机技术详解 | taoci-qiu-shaixuanji.jpg | 1200×675 |

**已落地改动**：
1. `images/external/` 下用 sips 把 7 张图缩到最长边 1200、jgp 85% 质量（单图 200-400KB）
2. `index.html` 7 个 src 全部替换（line 360/377/394/408/423/438/452）
3. `index.html` head 改 og:image / twitter:image / JSON-LD ImageObject URL（line 18/25/182-184）→ 全部指向金刚石选型机图（最具代表性，作为分享卡片图）
4. `image-sitemap.xml` 重写为 9 个 image:image（logo + placeholder.jpg + 7 新图）

**未做（保留原状）**：
- 4 个 Product JSON-LD 的 image 仍指 placeholder.jpg（4 个 Product 对应 4 个产品，但用户没给 4 张独立产品图；硬拆给 4 个 Product 不如保留 1 张"通用代表图"）
- 5 张无 width/height 的卡片图保持原样（依赖 .thumb-img.focus 的 CSS max-height 168 兜底；CSS 未限宽，1200×1200 src 在 4 列 grid 中会被容器 width 限制；视觉可能略压扁但可接受）
- alt 文本未改（用户没要求；alt 描述与图片主题不完全一一对应但 alt 文本已合理）

---

## Git 提交（用户指令"代码帮我提交一下"）

- commit `30fea1d`：SEO: 首页图片百度收录优化（图片sitemap+7张产品图+ImageObject JSON-LD）
- 10 files changed, 95 insertions(+), 10 deletions(-)
- 已 push 到 origin/main（`ffa8b4c..30fea1d`），**SSH 直推成功无超时**（本次沙箱代理未拦截 github.com）
- `.workbuddy/` 保持 untracked 未提交（内部工作数据，非网站文件）；项目无 .gitignore，若想彻底忽略可后续新建
